# Rationale Index — backend/

Kind: explanation

Local implementation reasoning for backend code lives here when a terse code warning needs durable context. Code pointers use `# why: docs/rationale/backend.md#<kebab-slug>`.

Entries are added only when implementation exists; each entry must explain the constraint or trade-off rather than restate the code and must end with a `Refs:` line.
