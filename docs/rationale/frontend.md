# Rationale Index — frontend/

Kind: explanation

Local implementation reasoning for frontend code lives here when a terse code warning needs durable context. Code pointers use `// why: docs/rationale/frontend.md#<kebab-slug>`.

Entries are added only when implementation exists; each entry must explain the constraint or trade-off rather than restate the code and must end with a `Refs:` line.
